# CurveSmear 初版仕様

## 合意した範囲

- 見た目と操作の基準は `demo/index.html`（Study 02）。
- Windows用AEエフェクトプラグイン（.aex）。AE 25.3を基準とし、26系でも検証する。
- 主な素材は1920×1080、PNGおよびマルチレイヤー／マルチチャンネルEXRを使う制作フロー。
- 入力画像の構図・位置を維持し、カーブとRadiusの影響範囲のみを変形する。
- 同じレイヤーの開いたマスクパスを1本指定する。マスクモード「なし」を想定。
- Smear量、Radius、境界のぼかし、筋の強さ・細かさ、元画像との混合、方向反転を用意する。
- 半径はカーブ全体で共通。追加マスクや複数カーブの合成は初版に含めない。
- 複数インスタンスはエフェクト順に処理する。
- パスと連続値のキーフレームアニメーションに対応する。ノイズは固定を基本としSeedを用意する。
- 出力領域をレイヤー境界の外へ拡張しない。

## 入力と品質の実装方針

- ファイルの直接読み込みではなく、AEから渡されるレイヤーのRGBAを処理する。
- マルチEXRのパス選択・抽出はAE側で行い、その結果にCurveSmearを適用する想定。EXR内の全チャンネルを一括変形する機能は含めない。
- 8/16/32bpcを対象とする。32bit floatではHDR値を0〜1へクランプしない。
- 透明境界を考慮した補間を行う。ブラウザの8bitデモとは別に検証する。
- Radius外、Smear量0、未指定パスでは入力を保持する。
- 複数セグメントの開いたベジェパスを想定する。閉じたパスの扱いを明示し、無言で異なる変形にしない。

## ローカル環境で確認済み

- SDK 25.2: `C:\SDK\Adobe\AE_SDK\25.2\AfterEffectsSDK`
- 追加SDK 25.6: `C:\SDK\Adobe\AE_SDK\AfterEffectsSDK_25.6_61_win\ae25.6_61.64bit.AfterEffectsSDK`
- AfterFX.exeの製品バージョンは、AE 2025が25.5、AE 2026が26.3。現環境で25.3実機検証は未確認。
- Visual Studio、MSVC、MSBuild、CMakeが利用可能。
- 追加SDKのPathMaster、SmartyPants、Skeletonサンプルを確認した。
- 25.6 SDKでのビルドだけではAE 25.3互換を保証しない。利用するAPIの版とホスト実機で確認する。

## 現在の実装と検証

- `.aex` のビルドとAE 25.5/26.3での読み込みを確認済み。
- マスクモード「なし」の開いたパスを取得し、Study 02の局所変形を移植済み。
- 8/16/32bpcのAEレンダーを確認済み。単体テストではHDR値、透過補間、範囲外保持、タイル描画、縮小表示座標を確認済み。
- ネイティブ8bpc出力とStudy 02の平均チャンネル差は約0.014/255。
- `PF_OutFlag2_I_MIX_GUID_DEPENDENCIES` を宣言し、パス形状をレンダーキャッシュキーへ含める。
- パス未指定時も依存ヘッダーを必ず混ぜ、エフェクト追加直後は入力をそのまま返す。
- 残作業は実素材での操作感、パスアニメーション、1920×1080の速度、EXR制作フローの確認。

25.3は手元の実行ファイルが25.5へ更新済みのため、実機では未確認。

## 次の改修についての合意（未実装）

- 高速化は後回し。約55msという使用時の報告を記録し、現段階では最適化しない。
- 最近傍補間を追加する方向。入力ピクセルを混ぜずに読み、0/1素材の色・アルファへ補間由来の中間値が生じるのを防ぐ。既存の線形補間との切り替えを候補とする。
- `Keep Original`の中間値による混合は今回の対応対象から外す。既存機能は変更しない。
- AEエフェクトにRound／Flat端の切り替えを追加することで合意。Flatは始点・終点の接線に直角な端を持つリボン状の領域とし、現在の丸い端による余分な広がりを抑える。
- Flat端の操作感は使用後に評価・調整する。端専用のぼかしや初期値などの詳細は未確定。
- この節はAE側の未実装項目を記録する。今回の設計相談ではネイティブコードは変更しない。
- WebデモStudy 03ではFlat／Round端の切り替えを実装済み。AEエフェクト側にはまだ移植していない。

## 検討中：刀身の根元から先端へ伸びを増やす（未決定・未実装）

- 要望：剣を振る画像で、刀身の根元は短く、剣先ほど長いsmearにする。
- ユーザー案：カーブの弧から自動判定する。
- 検討時は、運動軌道に沿う方向と、刀身の根元から先端へ向かう方向を区別する。弧状の運動では後者がリボンの幅方向に相当する配置がある。
- 提案候補A：リボン幅方向の強度勾配。片側を弱く、反対側を強くし、側の反転と勾配の調整を可能にする。Radiusの境界減衰とは別の制御として扱う。
- 提案候補B：根元・剣先の2点で強度勾配を指定。カーブ形状と独立して、どこを弱く／強くするかを明示できる。
- 提案候補C：回転中心と角度を指定する方式。純粋な平面回転なら移動弧長は中心からの距離に比例するが、遠近・並進を含む素材では演出的調整が必要。
- 自動判定では、直線に近い部分、S字、変曲点で曲率や内外の判定が不安定になり得る。自動推定のみを必須とせず、手動調整を残す案を検討する。
- 現在も法線方向のオフセットを保ってカーブをたどるため、円弧の内外で実移動距離に差が生じ得る。追加倍率が必要か、現状の幾何による差が足りないかは、刀身の素材とパス配置で確認する。

### 幅方向の倍率グラフ案（ユーザー提案・検討中）

- トーンカーブ状の編集グラフを用意する。横軸はリボンの幅方向のL端→中心→R端、縦軸はsmearの伸び倍率（%）。パスの始点→終点を横軸にはしない。
- 通常はL=100%、R=100%の水平線で既存の伸びを維持。提示図のようにLが刀身の根元側なら、L=1%、R=100%として幅方向に伸びを増やす。0%は変位なし、1%はごく小さい変位として区別する。
- 両端の数値入力と、中間点／曲線による配分調整を候補にする。直線なら均等に増加、曲線なら剣先側だけ急に増加する、といった演出を可能にする。
- L/Rはカーブに沿った一貫した法線の両側に対応させ、曲率から内側／外側を自動判定して入れ替えない。画面内のL/R表示と倍率分布の左右反転操作を候補にする。Reverseによるsmear方向の変更ではL/Rを入れ替えない。マスク自体の頂点順序を反転した場合の扱いは別途決める。
- 実装案は、符号付き幅位置を0〜1へ正規化してグラフを評価し、既存処理が計算するパス上の移動量shiftに倍率を掛ける。Amount自体への乗算は、現在の非線形なAmount→shift変換により指定%と変位倍率が一致しないため避ける。
- この倍率はパス上の変位を制御する。曲率と法線オフセットにより、画面上の実移動距離が厳密に同じ比率になるとは限らない。Radiusの影響範囲、Edge Feather、Keep Originalとは独立した制御として扱う。
- WebデモStudy 04で幅方向倍率グラフを実装済み。L/R数値入力、中間点の追加・移動・削除、形状を保つ曲線補間／直線補間、倍率分布の左右反転、3種のプリセット、刀身の比較シーンを用意した。
- デモではLを正の法線側（橙）、Rを負の法線側（青）に対応させた。刀身シーンではLが根元側。倍率はshiftに掛け、0%ではサンプリングをスキップして入力ピクセルを保持する。
- AE内のグラフ編集UIと保存・アニメーション方式は未設計・未実装。Webデモの操作感を見てから決定する。

### AE移植の相談：Study 04確認後

- ユーザー希望：Web版の倍率グラフと操作をAEエフェクト内へ移植する。「均一100%」ボタンは「リセット」に改名し、「L/Rの入れ替え」ボタンも含める。
- SDK 25.2のCustom_ECW_UI／ColorGridサンプルで、エフェクトコントロール内のカスタム描画・操作と任意データパラメータを確認。HTMLを直接表示する方式ではなく、同じ倍率計算と点編集をネイティブUIとして再現する方針。
- 提案する基本仕様：倍率0〜100%、初期値は100%の水平線。点追加・移動・削除、L/R数値入力、なめらかな曲線／直線切り替えを維持する。
- 「リセット」は倍率グラフの中間点を除き、L/Rを100%へ戻す。他の変形設定やマスクパスは保持。「L/Rの入れ替え」は中間点も含む倍率分布全体を左右反転する。
- Undo/Redo、プロジェクト保存・再読み込み、エフェクトの複製・コピー、プリセットへの保存で倍率データを保持できる実装を目指し、それぞれ検証する。
- 移植前の確認事項：グラフ形状自体のキーフレームアニメーションを初版から必要とするか。対応する場合は、キー間で点数が異なる場合を含め補間方法を定義する。Smear Amountなど既存パラメータのアニメーションとは別の事項。
- AE側の実装はこの相談では開始していない。
